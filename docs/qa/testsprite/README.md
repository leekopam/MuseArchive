# TestSprite Artifacts

Generated QA planning files are stored here so they do not stay mixed into the
project root.

Notes:
- Keep only stable planning outputs that are safe to share.
- Keep machine-local scratch files under `tmp/`, `cache/`, or `artifacts/` and do not commit them.
- Store committed reference outputs only as reviewed top-level files in this directory.
- Do not store secrets or API keys in this directory.
