org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
kotlin.incremental=false

# Permanent Android package name for this project.
musearchive.namespace=com.example.my_album_app
musearchive.applicationId=com.example.my_album_app
# Set this to true in CI/release pipelines if unsigned fallback must fail fast.
# musearchive.requireReleaseSigning=true

# Release signing is now configured locally, so keep release builds strict.
musearchive.requireReleaseSigning=true
